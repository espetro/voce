# Rust
target/
**/*.rs.bk

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Project-specific
# voce.app is the release template (tracked). Exclude only the compiled binary inside it.
voce.app/Contents/MacOS/
voce.app/Contents/_CodeSignature/
tmp/
.osgrep/

# Audio driver — compiled output
audio-driver/VoceAudio.driver/Contents/MacOS/

# Install — Vite dependency cache
install/.vite/

# Panel — dependencies (dist/ is committed so Rust can compile without building panel first)
assets/panel/node_modules/
assets/panel/.vite/

# Eval — generated fixtures and source data (download with just eval::setup, never commit)
eval/.venv/
eval/.env
eval/uv.lock
eval/fixtures/
eval/**/*.wav
eval/**/*.tar.gz
eval/**/*.jsonl
eval/scripts/__pycache__/
eval/fixtures/librispeech_*/
eval/fixtures/other/
eval/fixtures/alimeeting/
eval/fixtures/msc/
eval/fixtures/audio_tests/

# Python (eval/)
eval/.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
*.egg-info/
dist/
!assets/panel/dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*.bak
.osgrep
